[package]
name = "vdr_nif"
version = "0.1.0"
edition = "2021"

[lib]
name = "vdr_nif"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.36"
libc = "0.2"
bytes = "1.5"

[build-dependencies]
cc = "1.0"
