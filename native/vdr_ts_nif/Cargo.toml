[package]
name = "vdr_ts_nif"
version = "0.1.5"
edition = "2021"

[lib]
name = "vdr_ts_nif"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.37.2"
im = "15.1.0"
indexset = "0.15.0"
ordered-float = "5.1.0"
mlua = { version = "0.11.6", features = ["luajit", "vendored", "send"] }
rand = "0.10.0"
